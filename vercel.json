{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "SPA(단일 페이지 앱) 폴백 설정. /dashboard 같은 경로로 직접 접속/새로고침해도 404가 나지 않도록, 모든 경로 요청을 index.html로 돌려보낸다. 실제 라우팅은 브라우저에서 react-router가 처리한다.",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
